# --- pipeline ---
faster-whisper>=1.0.3
numpy>=1.26
opencv-python-headless>=4.9,<5   # 5.x removeu CascadeClassifier (Haar) do pacote principal

# --- download por URL do YouTube ---
# o YouTube exige um runtime JS para resolver os formatos; deno vem via pip
# Inclui correções recentes do TikTok e suporte a conexões de navegador.
yt-dlp[default,curl-cffi]>=2026.9.16.232951.dev0
deno>=2.5

# --- seleção de trechos (escolha o(s) que for usar) ---
anthropic>=0.40
openai>=1.40

# --- api ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
tzdata>=2024.1
